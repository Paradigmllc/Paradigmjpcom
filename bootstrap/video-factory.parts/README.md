# Temporary Video Factory staging

This directory contains SHA-pinned archive chunks used by the one-shot materialization workflow. The workflow validates, extracts, tests, commits the normal source tree, and removes this directory on success.
