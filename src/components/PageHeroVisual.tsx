import RouteVisualCard from "@/components/marketing-visuals/RouteVisualCard"

/**
 * Shared route-aware hero media. The imagery represents Paradigm's own delivery
 * systems and intentionally avoids implying client work or unsupported results.
 */
export default function PageHeroVisual() {
  return <RouteVisualCard />
}
