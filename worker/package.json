{
  "name": "paradigm-outreach-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Form outreach worker for Playwright Stealth x Crawlee. Runs only when the app delegates remote browser work; keep it off shared Droplet always-on mode.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node --import tsx src/index.ts",
    "install:browser": "playwright install chromium --with-deps",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@browserbasehq/stagehand": "^3.5.0",
    "crawlee": "^3.10.1",
    "playwright": "^1.60.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "comment_disk_safety": "Run npm install and playwright install chromium only during container builds. Do not install Chromium directly on the shared Droplet; use CDP_ENDPOINT for remote browser execution."
}
