---
import DemoLayout from "../layouts/DemoLayout.astro"
import { generateDemo, getIndustryLabel, getAppealLabel } from "../keystatic/demo-data"

const lang = (Astro.url.searchParams.get("lang") || "ja") as "ja" | "en"
const industry = Astro.url.searchParams.get("industry") || "consulting"
const appeal = Astro.url.searchParams.get("appeal") || "diagnostic"

const data = generateDemo(industry, appeal as "diagnostic" | "sales" | "brand" | "tech", lang)
const isJa = lang === "ja"
---

<DemoLayout title={`${data.customerName} — ${isJa?"改善デモ":"Demo"}`} accentColor={data.accentColor} accentColorDark={data.accentColorDark} accentColorLight={data.accentColorLight} lang={lang}>

  <!-- ═══════════════ HERO ═══════════════ -->
  <section className="relative min-h-[90vh] flex items-center overflow-hidden">
    <div className="orb orb-1 animate-float top-[-10%] left-[-10%]"></div>
    <div className="orb orb-2 animate-float-slow top-[40%] right-[-5%]"></div>
    <div className="absolute inset-0 opacity-15 bg-[url('data:image/svg+xml;base64,')]"></div>

    <div className="relative z-10 w-full max-w-7xl mx-auto section">
      <div className="max-w-3xl">
        <div className="flex items-center gap-3 mb-6">
          <div className="badge"><span className="badge-dot badge-dot-pulse"></span>{getIndustryLabel(industry, lang)}</div>
          <div className="badge">{getAppealLabel(appeal, lang)}</div>
        </div>
        <h1 className="text-display gradient-text mb-6">{data.heroHeadline}</h1>
        <p className="text-body text-lg md:text-xl mb-8">{data.heroSubtitle}</p>
        <div className="flex flex-col sm:flex-row gap-3 mb-14">
          <a href={data.calBookingUrl} target="_blank" rel="noopener noreferrer" className="btn btn-primary">
            {data.heroCta}
            <svg className="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
          </a>
          <a href="#services" className="btn btn-ghost">
            {isJa ? "改善内容を見る" : "See Improvements"}
            <svg className="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
          </a>
        </div>

        <!-- Browser Mockup -->
        <div className="browser-mock max-w-2xl">
          <div className="browser-bar">
            <span className="browser-dot"></span>
            <span className="browser-dot"></span>
            <span className="browser-dot"></span>
            <div className="browser-url"></div>
          </div>
          <div className="browser-content">
            <div className="flex items-center gap-3 mb-6">
              <div className="h-10 w-10 rounded-xl bg-brand/20 flex items-center justify-center text-brand-light text-sm font-black">P</div>
              <div>
                <div className="h-3 w-28 rounded-full bg-white/[.06] mb-2"></div>
                <div className="h-2.5 w-20 rounded-full bg-white/[.04]"></div>
              </div>
            </div>
            <div className="grid grid-cols-2 gap-3 mb-5">
              <div className="rounded-xl bg-white/[.03] border border-white/[.05] p-4">
                <div className="h-4 w-16 rounded-full bg-brand/20 mb-3"></div>
                <div className="h-2.5 w-full rounded-full bg-white/[.04] mb-2"></div>
                <div className="h-2.5 w-2/3 rounded-full bg-white/[.04]"></div>
              </div>
              <div className="rounded-xl bg-white/[.03] border border-white/[.05] p-4">
                <div className="h-4 w-12 rounded-full bg-brand/20 mb-3"></div>
                <div className="h-2.5 w-full rounded-full bg-white/[.04] mb-2"></div>
                <div className="h-2.5 w-3/4 rounded-full bg-white/[.04]"></div>
              </div>
            </div>
            {data.improvementPoints && data.improvementPoints.length > 0 && (
              <div className="space-y-2">
                {data.improvementPoints.slice(0, 3).map((pt: string) => (
                  <div className="flex items-center gap-2">
                    <div className="h-1.5 w-1.5 rounded-full bg-brand-light flex-shrink-0"></div>
                    <div className="h-2 w-48 rounded-full bg-white/[.04]"></div>
                  </div>
                ))}
              </div>
            )}
          </div>
        </div>

        {data.heroStats && (
          <div className="flex flex-wrap gap-8 pt-10 mt-10 border-t border-white/[.06]">
            {data.heroStats.map((s: { label: string; value: string; suffix: string }) => (
              <div>
                <div className="text-3xl font-black text-white tabular-nums">
                  {s.value}<span className="text-lg gradient-text ml-1">{s.suffix}</span>
                </div>
                <div className="mt-1 text-xs text-white/30">{s.label}</div>
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  </section>

  <!-- ═══════════════ LOGO WALL ═══════════════ -->
  <section className="section border-t border-white/[.03]">
    <div className="max-w-7xl mx-auto text-center">
      <p className="text-caption opacity-40 mb-8">{isJa ? "信頼されるパートナー" : "Trusted Partners"}</p>
      <div className="logo-wall flex flex-wrap items-center justify-center gap-x-14 gap-y-6">
        {[...(data.certifications || []), data.customerName].map((name: string) => <span>{name}</span>)}
      </div>
    </div>
  </section>

  <!-- ═══════════════ SERVICES ═══════════════ -->
  <section id="services" className="section">
    <div className="max-w-7xl mx-auto">
      <div className="text-center mb-16">
        <div className="badge mb-4"><span className="badge-dot"></span>Services</div>
        <h2 className="text-headline gradient-text mb-4">{data.serviceTitle}</h2>
        <p className="text-body max-w-2xl mx-auto">{data.serviceSubtitle}</p>
      </div>
      <div className="grid-3">
        {data.services.map((svc: { title: string; description: string; icon: string; features?: string[] }) => (
          <div className="card card-padded group">
            <div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-gradient-to-br from-brand to-brand-dark text-white text-xl shadow-lg shadow-brand/20 mb-5 group-hover:scale-110 transition-transform duration-300">
              {svc.icon === "Globe" ? "🌐" : svc.icon === "Search" ? "🔍" : "⚡"}
            </div>
            <h3 className="text-title text-white mb-2">{svc.title}</h3>
            <p className="text-body text-sm leading-relaxed mb-4">{svc.description}</p>
            {svc.features && svc.features.length > 0 && (
              <div className="flex flex-wrap gap-2">
                {svc.features.map((f: string) => (
                  <span className="text-[10px] font-medium px-2 py-0.5 rounded-full bg-white/5 text-white/40 border border-white/5">{f}</span>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════ CASE METRICS ═══════════════ -->
  <section className="section section-alt">
    <div className="max-w-7xl mx-auto">
      <div className="text-center mb-16">
        <div className="badge mb-4"><span className="badge-dot"></span>Results</div>
        <h2 className="text-headline gradient-text mb-4">{data.caseTitle}</h2>
        <p className="text-body max-w-2xl mx-auto">{data.caseDescription}</p>
      </div>
      <div className="grid-4" data-reveal>
        {data.caseMetrics.map((m: { label: string; value: string; suffix: string; detail?: string }) => (
          <div className="card card-padded text-center">
            <div className="text-4xl md:text-5xl font-black gradient-text mb-3">
              {m.value}<span className="text-xl ml-1">{m.suffix}</span>
            </div>
            <div className="text-sm font-semibold text-white/60 mb-1">{m.label}</div>
            {m.detail && <div className="text-xs text-white/30 leading-relaxed">{m.detail}</div>}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════ PROCESS ═══════════════ -->
  <section className="section">
    <div className="max-w-7xl mx-auto">
      <div className="text-center mb-16">
        <div className="badge mb-4"><span className="badge-dot"></span>Process</div>
        <h2 className="text-headline gradient-text mb-4">{data.processTitle}</h2>
      </div>
      <div className="grid-4">
        {data.processSteps?.map((step: { step: string; title: string; description: string; icon: string }, i: number) => (
          <div className="card card-padded text-center group relative">
            <div className="absolute top-3 right-4 text-[72px] font-black opacity-[.03] select-none leading-none">{i + 1}</div>
            <div className="flex h-14 w-14 mx-auto items-center justify-center rounded-2xl bg-gradient-to-br from-brand to-brand-dark text-white text-xl shadow-lg shadow-brand/20 mb-5 group-hover:scale-110 transition-transform duration-300">
              {step.icon === "Search" ? "🔍" : step.icon === "Zap" ? "⚡" : step.icon === "BarChart" ? "📊" : "🛡️"}
            </div>
            <div className="text-xs font-bold text-brand-light mb-2">{step.step}</div>
            <h3 className="text-title text-white mb-2">{step.title}</h3>
            <p className="text-body text-xs leading-relaxed">{step.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════ FAQ ═══════════════ -->
  <section className="section section-alt">
    <div className="max-w-3xl mx-auto">
      <div className="text-center mb-16">
        <div className="badge mb-4"><span className="badge-dot"></span>FAQ</div>
        <h2 className="text-headline gradient-text mb-4">{data.faqTitle}</h2>
      </div>
      <div className="space-y-3">
        {data.faqItems?.map((faq: { question: string; answer: string }, i: number) => (
          <details className="glass cursor-pointer group" open={i === 0}>
            <summary className="flex items-center justify-between p-5 font-semibold text-white/70 group-open:text-white list-none">
              <span className="flex items-center gap-3">
                <span className="text-brand-light text-sm font-bold">Q{i + 1}</span>
                {faq.question}
              </span>
              <svg className="h-4 w-4 text-white/20 group-open:rotate-180 transition-transform flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
              </svg>
            </summary>
            <div className="px-5 pb-5 pt-1 text-sm text-white/40 leading-relaxed">{faq.answer}</div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════ CTA ═══════════════ -->
  <section className="section">
    <div className="max-w-3xl mx-auto text-center">
      <div className="glass-strong p-12 md:p-20 relative overflow-hidden">
        <div className="absolute inset-0 opacity-[.07] bg-[radial-gradient(circle_at_50%_50%,var(--brand),transparent_70%)]"></div>
        <div className="relative z-10">
          <h2 className="text-headline gradient-text mb-6">{data.ctaTitle}</h2>
          <p className="text-body mb-10 max-w-xl mx-auto">{data.ctaBody}</p>
          <a href={data.calBookingUrl} target="_blank" rel="noopener noreferrer" className="btn btn-primary text-lg">
            {data.heroCta}
            <svg className="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>

</DemoLayout>
